Drop the four official TSEF PNGs here, then run:

```bash
bash scripts/install-official-assets.sh
```

Required filenames:

- `tsef-logo-official.png`
- `tsef-b-transparent.png`
- `tsef-b-badge-only.png`
- `tsef-b-banner.png`
